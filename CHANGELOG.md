# Changelog

## Unreleased

- **新增：根目录 `LICENSE`（MIT）**。项目此前在 `package.json` / README 中声明 MIT 但缺少许可证文件，现补齐并随构建复制到 `dist/` 与 `package.zip`。
- **新增：设置面板底部的「重置设置」按钮**。点击后弹出思源原生警告确认框（`confirm`），确认后把所有设置恢复为默认值（含默认图标集），并立即刷新设置界面与持久化到 `settings.json`。
- **新增：默认启用几个常用彩色图标集**。新安装用户会默认勾选 `flat-color-icons`、`icon-park`、`twemoji`、`fluent-emoji-flat`、`logos`、`skill-icons`、`vscode-icons` 这 7 个集合（见 `types.ts` 的 `DEFAULT_COLLECTIONS`），开箱即有好看且覆盖广的搜索结果；全部取消勾选仍表示使用全部图标集。已保存过设置的用户不被强制修改。
- **移除：顶部工具栏按钮**（`addTopBar`）。不再在顶栏提供「为当前文档设置图标」入口，请改用文档树 / 文档标题图标右键菜单，或点击文档树图标打开的原生面板。相关的 `openPickerForCurrentDoc`、`lastProtyle` 跟踪（含 `destroy-protyle` 钩子）与 `topbarTitle` 文案一并清理。

## 0.3.0

- **新增：图标集偏好设置**。在设置面板中用复选框选择只使用哪些 Iconify 图标集；未勾选表示全部。原生面板、独立选择器、按标题自动匹配、新文档自动匹配都会在选定范围内搜索。
- 图标集多选框支持搜索、全选、清空、反选，并显示每个图标集的图标数量。

## 0.2.0

- **新增：注入思源原生图标面板**。点击文档树里文件 / 笔记本前面的图标、文档标题图标时，弹出的原生面板中搜索即可显示 Iconify 结果，点击即可切换，体验与原生 emoji 一致。
- 修复：`plugin.json` 的 `name` 与目录名不一致导致思源无法识别（集市包安装目录名与清单文件中的 name 字段值不一致）。
- 新增：构建时校验目录名与 `plugin.json` 的 `name`，不一致直接报错。
- 补齐 `plugin.json` 的 `kernels` / `icon` / `preview` 字段。

## 0.1.0

- 首个版本
- 支持从 [Iconify](https://icon-sets.iconify.design/) 搜索 / 按图标集浏览图标，并为文档设置图标
- 文档树右键菜单：设置图标、含子文档设置、移除图标
- 文档标题图标菜单：设置 / 移除图标
- 顶部工具栏按钮：为当前文档设置图标
- 笔记本右键菜单：批量设置文档图标
- 按文档标题自动匹配图标，支持「仅处理无图标的文档」
- 可选：打开无图标文档时自动按标题匹配
- 颜色选择（调色板 / 取色器 / 保留原色），颜色固化到 SVG 文件
- 最近使用、加载更多
- 设置面板：搜索数量、默认颜色、分组目录、防抖、并发数、自动匹配开关
